To change I/O schedulers you need to go "cat /sys/block/hdc/queue/scheduler". To display all of the available schedulers. The shceduler currently in use will be displayed within brackets.
To change to a different scheduler "echo SCHEDULER_NAME > /sys/block/hdc/queue/scheduler".


1.) Write LOOK module
2.) place in directory
3.) update block/Kconfig.iosched, block/Makefile 
4.) rebuild the kernel
5.) change to the scheduler
6.) run python script
